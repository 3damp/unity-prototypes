var GHPATH = '/unity-prototypes/super-drawman-old';
var APP_PREFIX = 'sdmo_';
var VERSION = 'v_00';

var URLS = [
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/loader.js`
  `${GHPATH}/Build/`,
  `${GHPATH}/Build/game.data`,
  `${GHPATH}/Build/game.framework.js`,
  `${GHPATH}/Build/game.wasm`,
]